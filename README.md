Simple app for learning foreign languages.

Create file `data.json` with phrases that you want to learn. See root of the project for an example.

Run `java -jar simple-quiz-1.0.jar` in a folder with .json file to launch quiz.

Use `assembly:single` to create new .jar

Jar version migth not work properly on Windows CMD.